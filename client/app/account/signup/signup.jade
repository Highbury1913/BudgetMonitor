div(ng-include='"app/navbar/navbar.html"')

md-content(flex)
  div(layout="column", layout-fill, layout-align="space-around center")
    .md-whiteframe-z2(style="max-width:600px;width:90%;margin:35px;")
      section
        span
          md-toolbar.md-accent.md-default-theme
            div.row
              h2.md-toolbar-tools(layout="column", layout-align="start center")
                span(style="padding: 12px; margin-top: -10px;")
                  md-icon(md-svg-src="assets/images/signup.svg")
                span(style="padding: 12px 0;") Sign up
          md-content.md-padding(layout="row", layout-sm="column", layout-align="center center")
            span(style="padding:15px")
              md-button.md-warn.md-raised(href='', ng-click='loginOauth("google")', style="padding:5px")
                |  Connect with Google+
          md-content(layout="row", layout-align="center center")
            span
              | - OR -
          md-content.md-padding(layout="column")
            form(name="signUpForm", ng-submit="register(signUpForm)")
              md-input-container
                label Name
                input(required, name="name", ng-model="user.name")
                div(ng-messages="signUpForm.name.$error")
                  div(ng-message="required")
                    | A name is required to sign up.
              md-input-container
                label Email
                input(required, type="email", name="email", ng-model="user.email")
                div(ng-messages="signUpForm.email.$error")
                  div(ng-message="required")
                    | An email is required.
                  div(ng-message="email")
                    | The email address is not valid.
                  div(ng-message="mongoose")
                    | {{ errors.email }}
              md-input-container
                label Password
                input(type="password", required, name="password", ng-model="user.password", ng-minlength='3', mongoose-error='')
                div(ng-messages="signUpForm.password.$error")
                  div(ng-message="required")
                    | A password is required.
                  div(ng-message="minlength")
                    | Your password needs to be at least 3 characters long.
                  div(ng-message="mongoose")
                    | {{ errors.password }}
              div(layout="row", layout-align="end center")
                span(style="padding: 5px;")
                  md-button.md-raised.md-primary(type="submit")
                    | Sign up
                span(style="padding: 5px;")
                  md-button.md-raised(href='/login')
                    | Log in
